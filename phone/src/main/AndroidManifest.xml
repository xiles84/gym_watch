<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".PhoneApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".PhoneActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Not for notifications. Holding notification access is what lets an
             app see other apps' media sessions, Audible's among them
             (docs/LESSONS.md #32). The system binds it once the user allows it,
             and keeps the process alive while bound. -->
        <service
            android:name=".AudiobookListenerService"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Play requests from the watch. The path must match
             AudiobookWire.PLAY_PATH, or requests never reach this service. -->
        <service
            android:name=".PlayRequestService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.REQUEST_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/gymwatch/audiobooks/play"
                    android:scheme="wear" />
            </intent-filter>
        </service>
    </application>
</manifest>
