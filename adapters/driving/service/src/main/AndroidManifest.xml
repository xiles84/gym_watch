<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- specialUse only. The health type additionally requires a runtime
         permission to be *granted* before startForeground, and a chronometer has
         no business claiming it (docs/LESSONS.md #19). Samsung Health records
         workouts; this service only keeps timers alive. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <service
            android:name=".GymSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps a gym rest timer and chronometer running while the screen is off" />
        </service>
    </application>
</manifest>
